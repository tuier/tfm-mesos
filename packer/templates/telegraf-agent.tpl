[agent]
  interval = "5s"
  round_interval = true
  metric_buffer_limit = 1000
  flush_buffer_when_full = true
  collection_jitter = "1s"
  flush_interval = "5s"
  flush_jitter = "1s"
  quiet= true
  omit_hostname = true
